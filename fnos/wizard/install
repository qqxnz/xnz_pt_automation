[
  {
    "stepTitle": "${install.step_port_title}",
    "items": [
      {
        "type": "text",
        "field": "wizard_service_port",
        "label": "${install.label_service_port}",
        "initValue": "3180",
        "rules": [
          { "required": true, "message": "${install.service_port_required}" },
          { "pattern": "^[0-9]{2,5}$", "message": "${install.service_port_pattern}" }
        ]
      }
    ]
  }
]
