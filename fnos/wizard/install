[
  {
    "stepTitle": "${install.step_port_title}",
    "items": [
      {
        "type": "text",
        "field": "wizard_service_port",
        "label": "${install.label_service_port}",
        "initValue": "3180",
        "rules": [
          { "required": true, "message": "${install.service_port_required}" },
          { "pattern": "^[0-9]{2,5}$", "message": "${install.service_port_pattern}" }
        ]
      }
    ]
  },
  {
    "stepTitle": "${install.step_account_title}",
    "items": [
      {
        "type": "text",
        "field": "wizard_admin_password",
        "label": "${install.label_admin_password}",
        "initValue": "123456",
        "rules": [
          { "required": true, "message": "${install.password_required}" }
        ]
      },
      {
        "type": "text",
        "field": "wizard_image_tag",
        "label": "${install.label_image_tag}",
        "initValue": "0.6.8",
        "rules": [
          { "required": true, "message": "${install.image_tag_required}" }
        ]
      }
    ]
  }
]
