services:
  xnz-pt-automation:
    image: crpi-yg64rrvs864jdm4p.cn-shenzhen.personal.cr.aliyuncs.com/qqxnz/xnz-pt-automation:${wizard_image_tag:-0.6.14}
    container_name: xnz-pt-automation
    restart: unless-stopped
    network_mode: host
    environment:
      PORT: "${wizard_service_port:-3180}"
      DATA_DIR: /data
      DEFAULT_ADMIN_PASSWORD: "${wizard_admin_password:-123456}"
      TZ: Asia/Shanghai
    volumes:
      - ../var/app:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:${wizard_service_port:-3180}/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
